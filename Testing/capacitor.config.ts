import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'e2e-example-tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
